language: csharp
mono: none
dotnet: 2.1.605

env:
  - DOTNET_SKIP_FIRST_TIME_EXPERIENCE=1 DOTNET_CLI_TELEMETRY_OPTOUT=1

#cache:
#  directories:
#    - $TRAVIS_BUILD_DIR/snaps-cache

os:
  - linux
  - osx

install:
  - pwsh -File ./Tools/CI/Travis/Invoke-TravisInstall.ps1

script:
  - pwsh -File ./Tools/CI/Travis/Invoke-TravisScript.ps1

addons:
  snaps:
  - name: powershell
    confinement: classic
  homebrew:
    casks:
    - powershell