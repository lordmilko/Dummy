language: csharp
mono: none
dotnet: 2.1.605

#cache:
#  directories:
#    - $TRAVIS_BUILD_DIR/snaps-cache

matrix:
  include:
    - os: linux
    - os: osx
      osx_image: xcode10.2

install:
  - pwsh -c "ipmo ./Tools/CI/Travis.psm1; Invoke-TravisInstall"

script:
  - pwsh -c "ipmo ./Tools/CI/Travis.psm1; Invoke-TravisScript"

addons:
  snaps:
  - name: powershell
    confinement: classic
  homebrew:
    casks:
    - powershell